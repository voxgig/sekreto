# Releases a sekreto npm package: publishes to npm AND writes the git tag.
#
# Modelled on voxgig/apidef's publish.yml and the reasoning in that repo's
# docs/how-to/release-and-tag.md; the three-job shape below matches
# voxgig/omni's. The comments restate the parts that are load-bearing here.
#
# ONE FILE, BECAUSE NPM ALLOWS EXACTLY ONE. A trusted publisher is registered
# on npmjs.com against a single workflow FILENAME, so anything that must
# accompany a publish - here, the tag - has to live inside it. A second
# workflow cannot publish whatever permissions it holds: npm rejects the
# exchange and reports it as a 404, not a 403, so as not to leak whether the
# package exists. They cannot be split across two FILES either: a ref pushed
# with GITHUB_TOKEN starts no further workflow run, so "tag in A, publish on
# the tag" publishes nothing, silently.
#
# THREE JOBS, EACH WITH THE LEAST IT NEEDS. This is the security shape of the
# file, and it is not tidiness:
#
#   build    contents: read.   Runs npm install, the build, the tests and the
#                              packaging checks - all the project code and
#                              every dependency lifecycle script. Ends by
#                              uploading the TARBALL.
#   publish  id-token: write.  Downloads that tarball and publishes it. NO
#                              CHECKOUT AT ALL, so no repository code and no
#                              dependency script can run here.
#   tag      contents: write.  Runs git and nothing else.
#
# Why it matters: `id-token: write` is a JOB-level grant. It puts
# ACTIONS_ID_TOKEN_REQUEST_URL and _TOKEN in the environment of every process
# in that job, so a compromised postinstall during `npm install` could mint
# the same OIDC token the publish uses and exchange it for a registry
# credential scoped to whatever packages trust this repo. Keeping install and
# build out of the OIDC job is what closes that, and `checkout` is omitted
# there for the same reason - it would persist a git credential too.
#
# PUBLISHING THE TARBALL, NOT THE DIRECTORY, is what makes the split possible
# and is better anyway: the exact bytes that the build job produced and
# tested are the bytes that ship, with no chance of the publish job's tree
# differing from the tested one. (voxgig/omni goes further and verifies the
# tarball itself with `make pack-check` / `make pack-diff`; those are worth
# porting here.) Provenance survives it -
# libnpmpublish computes the attestation subject from the tarball bytes
# (`ssri.fromData(tarballData)`), not from how the tarball was produced.
#
# TRUSTED PUBLISHING IS PER PACKAGE. @voxgig/sekreto and @voxgig/sekreto-js
# each need their own `npm trust` run, or a release fails with that
# misleading 404.
#
# THE CONFORMANCE TESTS ARE OMNI TESTS, so the build job checks voxgig/omni
# out as a sibling and points OMNI_HOME at it. That checkout lives in BUILD,
# never in the publish job - it is third-party code, and the whole point of
# the split is that no such code runs beside the OIDC token.
#
# VERSION BUMPS ARE NOT AUTOMATED, deliberately. Nothing here commits. It
# reads the version already on the branch and releases exactly that, so the
# bump stays a reviewable diff and the release stays a button.
#
# Do NOT run `npm run repo-publish` locally: it publishes over a token and
# bypasses OIDC entirely.

name: release

on:
  push:
    tags:
      - 'typescript/v*'
      - 'javascript/v*'
  workflow_dispatch:
    inputs:
      port:
        description: 'Port to release'
        required: true
        type: choice
        options: [typescript, javascript]

jobs:
  # Everything that runs project code or third-party scripts. No id-token,
  # no contents: write - if anything here is compromised it has neither a
  # registry credential nor a repository one to reach for.
  build:
    name: build and check
    runs-on: ubuntu-latest
    timeout-minutes: 20

    permissions:
      contents: read

    outputs:
      port: ${{ steps.resolve.outputs.port }}
      version: ${{ steps.resolve.outputs.version }}
      name: ${{ steps.resolve.outputs.name }}
      tag: ${{ steps.resolve.outputs.tag }}
      tarball: ${{ steps.pack.outputs.tarball }}
      published: ${{ steps.registry.outputs.published }}

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0

      # The conformance tests are omni tests, as in ci.yml. This checkout is
      # third-party code and belongs in this job, which holds no token.
      - name: Check out voxgig/omni
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: voxgig/omni
          path: .omni

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24.x

      # RELEASES COME FROM main, INCLUDING FROM THE BUTTON. `gh workflow run
      # --ref` accepts any branch, so without this the Actions UI could
      # publish a feature branch, irreversibly.
      - name: Dispatches must come from main
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: |
          set -euo pipefail
          [ "$GITHUB_REF_NAME" = "main" ] || {
            echo "::error::dispatch ref is $GITHUB_REF_NAME; releases must come from main"
            exit 1
          }

      - name: Resolve the port and version being released
        id: resolve
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            PORT='${{ inputs.port }}'
          else
            PORT="${GITHUB_REF_NAME%%/*}"
          fi
          case "$PORT" in
            typescript|javascript) ;;
            *) echo "::error::cannot resolve a port from '$GITHUB_REF_NAME'"; exit 1 ;;
          esac

          VERSION="$(node -p "require('./$PORT/package.json').version")"
          NAME="$(node -p "require('./$PORT/package.json').name")"
          test -n "$VERSION" || { echo "::error::no version in $PORT/package.json"; exit 1; }

          {
            echo "port=$PORT"
            echo "version=$VERSION"
            echo "name=$NAME"
            echo "tag=$PORT/v$VERSION"
          } >> "$GITHUB_OUTPUT"
          echo "$NAME $VERSION -> tag $PORT/v$VERSION"

      # A tag pushed by hand must agree with the version it claims. Without
      # this, pushing typescript/v0.1.2 while package.json still says 0.1.1
      # resolves 0.1.1, finds it already published, skips the publish and goes
      # GREEN - leaving a tag with no release behind it.
      - name: A pushed tag must match the package version
        if: ${{ github.event_name == 'push' }}
        env:
          TAG: ${{ steps.resolve.outputs.tag }}
        run: |
          set -euo pipefail
          [ "$GITHUB_REF_NAME" = "$TAG" ] || {
            echo "::error::tag $GITHUB_REF_NAME does not match package.json ($TAG)"
            exit 1
          }

      # A TAG CAN BE PUSHED FROM ANY BRANCH, and the dispatch guard above does
      # not cover that path. `git tag typescript/v0.1.1 && git push --tags`
      # from a feature branch arrives here with a ref that satisfies the
      # version check, and publishes that branch irreversibly. Releases come
      # from main on BOTH paths.
      #
      # --is-ancestor, not "equals the tip": a tag legitimately lags main by
      # the time the run starts, and that must still release.
      - name: A pushed tag must point at a commit on main
        if: ${{ github.event_name == 'push' }}
        run: |
          set -euo pipefail
          # An explicit refspec: a tag-push checkout has no origin/main, and
          # a bare `git fetch origin main` only writes FETCH_HEAD.
          git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
          git merge-base --is-ancestor HEAD refs/remotes/origin/main || {
            echo "::error::$GITHUB_REF_NAME points at a commit that is not on main"
            exit 1
          }
          echo "$GITHUB_REF_NAME is on main"

      # AN EXISTING TAG IS ONLY A PROBLEM IF IT POINTS SOMEWHERE ELSE. A tag
      # already on THIS commit is the idempotent case - nothing to create, not
      # an error - which is what makes re-dispatching after a partial release
      # safe.
      - name: Refuse a tag that points at a different commit
        if: ${{ github.event_name == 'workflow_dispatch' }}
        env:
          TAG: ${{ steps.resolve.outputs.tag }}
        run: |
          set -euo pipefail
          HEAD_SHA="$(git rev-parse HEAD)"
          AT="$(git rev-parse -q --verify "refs/tags/$TAG^{commit}" 2>/dev/null || true)"
          if [ -n "$AT" ] && [ "$AT" != "$HEAD_SHA" ]; then
            echo "::error::tag $TAG already exists on $AT, not this commit - bump the version"
            exit 1
          fi
          [ -z "$AT" ] || echo "$TAG already points here; nothing to create"

      # THE REGISTRY IS THE SOURCE OF TRUTH FOR "IS THIS RELEASED", not the
      # tag. A run can publish and then fail before tagging, leaving a version
      # on npm with nothing pointing at it. Without this check that state is
      # unrecoverable: the publish dies on "cannot publish over the previously
      # published versions" before the tag job ever runs.
      - name: Is this version already on npm?
        id: registry
        env:
          NAME: ${{ steps.resolve.outputs.name }}
          VERSION: ${{ steps.resolve.outputs.version }}
        run: |
          set -euo pipefail
          if npm view "$NAME@$VERSION" version >/dev/null 2>&1; then
            echo "published=true" >> "$GITHUB_OUTPUT"
            echo "$NAME@$VERSION is already on npm - skipping publish"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
            echo "$NAME@$VERSION is not on npm - will publish"
          fi

      # SKIPPING THE PUBLISH ASSUMES "SAME VERSION MEANS SAME CODE". That
      # holds only if main has not moved: publish at commit A, tag job fails,
      # main advances to B, someone re-dispatches - the publish is skipped and
      # B gets tagged, while the registry artifact was built from A. npm
      # records the source commit as gitHead, so this is checkable. Absent
      # gitHead cannot be verified, so warn rather than block: refusing would
      # make legitimate recovery impossible.
      - name: A published version must come from this commit
        if: ${{ steps.registry.outputs.published == 'true' }}
        env:
          NAME: ${{ steps.resolve.outputs.name }}
          VERSION: ${{ steps.resolve.outputs.version }}
        run: |
          set -euo pipefail
          GH="$(npm view "$NAME@$VERSION" gitHead 2>/dev/null || true)"
          if [ -z "$GH" ]; then
            echo "::warning::npm records no gitHead for $VERSION; cannot verify it was built from this commit"
          elif [ "$GH" != "$GITHUB_SHA" ]; then
            echo "::error::npm has $VERSION built from $GH, but this run is $GITHUB_SHA - tagging would point at code the registry does not contain"
            exit 1
          else
            echo "$VERSION on npm was built from this commit"
          fi

      # `make test-<port>`, not a bare `npm test`: the port Makefile's test
      # target installs and builds first, and a fresh checkout has no dist/.
      - name: Build and test
        env:
          OMNI_HOME: ${{ github.workspace }}/.omni
        run: make test-${{ steps.resolve.outputs.port }}

      # What is about to ship, named in the log, so a bad `files` list is
      # visible in the run rather than discovered on npm. voxgig/omni has
      # `make pack-check` and `make pack-diff` doing this properly - worth
      # porting here, but not in the change that split the jobs.
      - name: What will ship
        if: ${{ steps.registry.outputs.published == 'false' }}
        working-directory: ${{ steps.resolve.outputs.port }}
        run: npm pack --dry-run

      # The artifact the publish job will ship. Packing HERE, in the job that
      # ran the tests and the checks, is what makes "the bytes that were
      # verified are the bytes that ship" true rather than aspirational.
      - name: Pack
        id: pack
        if: ${{ steps.registry.outputs.published == 'false' }}
        working-directory: ${{ steps.resolve.outputs.port }}
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/dist"
          TARBALL="$(npm pack --pack-destination "$RUNNER_TEMP/dist" --silent | tail -1)"
          echo "tarball=$TARBALL" >> "$GITHUB_OUTPUT"
          echo "packed $TARBALL"
          tar tzf "$RUNNER_TEMP/dist/$TARBALL"

      - name: Upload the tarball
        if: ${{ steps.registry.outputs.published == 'false' }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: npm-tarball
          path: ${{ runner.temp }}/dist/${{ steps.pack.outputs.tarball }}
          if-no-files-found: error
          retention-days: 1

  # THE ONLY JOB WITH id-token: write, and it deliberately does NOT check the
  # repository out. It downloads the tarball build produced and publishes it.
  # No project code, no dependency lifecycle script and no git credential is
  # in scope while the OIDC token exists.
  publish:
    name: npm publish
    needs: build
    if: ${{ needs.build.outputs.published == 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24.x
          registry-url: 'https://registry.npmjs.org'

      - name: Download the tarball
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: npm-tarball
          path: ${{ runner.temp }}/dist

      # Trusted publishing requires npm >= 11.5.1, and node-version alone does
      # not guarantee it. `--ignore-scripts` because this is the one install
      # that happens beside the OIDC token: npm itself has no lifecycle
      # scripts, so this costs nothing and closes the last script path.
      - name: Use a trusted-publishing capable npm
        run: npm install -g npm@latest --ignore-scripts

      # No --provenance: trusted publishing attaches it on its own, and the
      # attestation subject is computed from these tarball bytes.
      - name: Publish to npm
        env:
          VERSION: ${{ needs.build.outputs.version }}
          TARBALL: ${{ needs.build.outputs.tarball }}
        run: |
          set -euo pipefail
          # A PRERELEASE MUST NOT BECOME `latest`. `npm publish` defaults to
          # the latest dist-tag, so publishing 0.2.0-beta.1 that way hands it
          # to every unpinned install.
          case "$VERSION" in
            *-*) DIST=next ;;
            *)   DIST=latest ;;
          esac
          echo "publishing $TARBALL under dist-tag $DIST"
          npm publish "$RUNNER_TEMP/dist/$TARBALL" --access public --tag "$DIST"

  # Runs git and nothing else. Skipped on the push path: the tag already
  # exists there. `always()` because publish is SKIPPED when the version is
  # already on npm, and that is the recovery case this job exists for - a run
  # that published and then failed before tagging.
  tag:
    name: tag
    needs: [build, publish]
    if: >-
      ${{ always()
          && github.event_name == 'workflow_dispatch'
          && needs.build.result == 'success'
          && (needs.publish.result == 'success' || needs.publish.result == 'skipped') }}
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0

      - name: Tag the release
        env:
          TAG: ${{ needs.build.outputs.tag }}
        run: |
          set -euo pipefail
          HEAD_SHA="$(git rev-parse HEAD)"

          # EXISTENCE IS NOT ENOUGH. `--verify` only asks whether the name
          # resolves; a tag created between build's guard and this checkout
          # could point anywhere. Treating that as idempotent would leave a
          # green run with an artifact from THIS commit and a release tag on a
          # different one.
          AT="$(git rev-parse -q --verify "refs/tags/$TAG^{commit}" 2>/dev/null || true)"
          if [ -n "$AT" ]; then
            if [ "$AT" != "$HEAD_SHA" ]; then
              echo "::error::$TAG exists on $AT, not this commit ($HEAD_SHA)"
              exit 1
            fi
            echo "$TAG already points here - nothing to do" >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          git tag "$TAG"
          git push origin "refs/tags/$TAG"
          echo "pushed $TAG" >> "$GITHUB_STEP_SUMMARY"
