# The corpus package builds in its own target dir, and its lockfile is
# derived from a tag-pinned git dependency, so neither is committed. Same
# as voxgig/struct's rust/corpus entries.
/corpus/target/
/corpus/Cargo.lock
