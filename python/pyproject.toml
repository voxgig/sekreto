[project]
name = "voxgig-sekreto"
version = "0.1.0"
description = "sekreto: one interface for secrets, wherever they live"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["voxgig_sekreto"]
